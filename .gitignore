node_modules
.next
out
dist-electron
release
.env.local
.env
*.log
.DS_Store
next-env.d.ts

# Real customer data — keep on disk for import dev, never commit
*.xlsx

# Claude Code local session settings
.claude/

# Supabase CLI local state (project ref, pooler URL, versions) — not for the repo
supabase/.temp/

# TS incremental build info
*.tsbuildinfo

# Local smoke-test scratch (not part of the app)
.smoke
